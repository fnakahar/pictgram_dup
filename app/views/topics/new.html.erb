<div class="topic-new-wrapper">
  <div class="container">
    <div class="row">
<!-- オフセットの書いてる順番は関係ない。左端から何カラムずらすかを指定しているだけ。この場合、左端から3カラムずらし、そのあと６カラム分を表示箇所としている。3-6-(3)-->
      <div class="col-md-6 col-md-offset-3">
        <h1 class="text-center">Add Picture</h1>
        <%= form_for @topic do |f| %>
          <div class="form-group">
            <%= f.label :image %>
            <%= f.file_field :image %>
          </div>
          <div class="form-group">
            <%= f.label :description %>
            <%= f.text_area :description, class: 'form-control' %>
          </div>

          <%= f.submit '投稿', class: 'btn btn-black btn-block' %>
          <% end %>
      </div>
    </div>
  </div>
</div>
